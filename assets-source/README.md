# assets-source

Raw AI-generated and draft source assets live here.

This directory is intentionally separate from runtime assets.
Only approved/processed assets should be copied into `src/assets/processed/`.
