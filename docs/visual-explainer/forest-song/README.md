# Start here

The agreed storybook is [index.html](index.html) (also saved as [storybook-v2.html](storybook-v2.html)), with the authoritative implementation brief in [docs/STORYBOOK.md](../../STORYBOOK.md) and structured video scripts in [storybook-v2.json](storybook-v2.json).

This revision centers language learning, defines the agreed shared selectable cast concepts, specifies wordless generated videos, and ends with a forest gathering. It includes the two-destination entrance concept and an art-reuse / pilot-production plan. No images or videos were generated for this revision. It does not implement the entrance or change the running game.

The current `index.html` and `script.json` contain the agreed storybook and its nine video scripts. The archived `index-v1.html`, `script-v1.json`, `prompts.json`, `verification.md` and preserved `assets/` document the earlier visual exploration; only those archived materials are historical references. They do not override the current brief or scripts. Keep all owl and flight imagery, while following the revised reunion ending.

## Verification

- Nine milestones validated: opening, 5, 10, 15, 20, 25, 30, 35, 36.
- Twenty shot briefs total 120 seconds of proposed edited runtime; every shot has visual and sound direction.
- Every video has a learning connection, world consequence, anticipation and production note.
- Embedded JavaScript syntax passed. All nine navigation states and first/last boundaries passed live browser checks.
- Three reference images loaded from embedded data. Font is embedded; no external media, scripts or styles are required.
- Desktop 1440 × 1000 and mobile 390 × 844 inspected; no page overflow. No browser errors observed.
- Story direction and cast concepts are agreed. Final character artwork, video model, pilot quality and production spending remain review items. No live video-generation capability or cost was tested.

## Approval update checks

Canonical and versioned HTML are identical; embedded JavaScript parses; both current script files match and contain the nine expected milestones. This update changes document status and entry points only. Earlier browser checks above cover the unchanged layout and navigation. No media was generated.

## Final implementation handoff

Story and final-act continuity are now specified in docs/STORYBOOK.md, including triggers, skip/replay behavior, journey save isolation, asset reuse, a bounded video pilot and acceptance checks. All original images—including owl and flight art—are preserved unchanged. The revised reunion scripts govern the final act. No paid generation or game implementation ran in this step.

## Maintaining scene consistency

Edit scene data in `storybook-v2.json`, then run `node docs/visual-explainer/forest-song/sync-storybook.mjs --write` from the repository root. Run the same command with `--check` to detect drift. Product and production rules live in docs/STORYBOOK.md; keep explanatory HTML prose aligned when changing those rules.
